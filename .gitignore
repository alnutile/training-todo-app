# Dependencies
node_modules

# Build output
dist

# Local env files (never commit secrets — ship .env.example instead)
.env
.env.local
.env.*.local

# Test output
playwright-report
test-results
blob-report
coverage
# Anchored: a bare `ui-review` would also swallow e2e/ui-review/ and
# scripts/ui-review/, which are source.
/ui-review/
.env_local_secrets_only
# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode
.idea

# TS build info
*.tsbuildinfo

# Supabase CLI local state
supabase/.temp
supabase/.branches
