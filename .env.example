# Placeholder env file for later steps. Copy to .env and fill in real values.
# Only VITE_-prefixed vars reach the browser — treat them as PUBLIC.
#
# Supabase (added in a later step):
# VITE_SUPABASE_URL=your-project-url
# VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
#
# NEVER put the Supabase service_role key here or in any VITE_ var — it is
# server-only and must never reach the client bundle.
